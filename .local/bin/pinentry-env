#!/usr/bin/env sh
# choose pinentry depending on PINENTRY_USER_DATA

case $PINENTRY_USER_DATA in
mac)
  # exec pinentry-mac "$@"
  exec /run/current-system/sw/bin/pinentry-mac "$@"
  ;;
*)
  # exec pinentry-tty "$@"
  exec /run/current-system/sw/bin/pinentry-tty "$@"
esac
